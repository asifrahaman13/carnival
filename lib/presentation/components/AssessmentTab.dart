import 'package:flutter/material.dart';

class AssessmentTab extends StatelessWidget {
  const AssessmentTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Assessment Tab'),
    );
  }
}