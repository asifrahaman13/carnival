// ignore: file_names
import 'package:flutter/material.dart';

class WearableTab extends StatelessWidget {
  const WearableTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Wearable Tab'),
    );
  }
}